import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Placeholder test', (WidgetTester tester) async {
    // App requires Firebase initialization – integration tests handle that.
    // This is just a placeholder to keep the test file valid.
    expect(true, isTrue);
  });
}
